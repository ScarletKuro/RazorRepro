@namespace Repro.Shared

<MudDataGrid T="MyViewModel">
    <Columns>
        <MyColumn Property="x => x.MyProperty">
            <CellTemplate>X</CellTemplate>
        </MyColumn>
    </Columns>
</MudDataGrid>

@code {
    List<MyViewModel> items = new() { new MyViewModel( ) };
}